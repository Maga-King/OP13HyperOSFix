# 一加 13 移植澎湃 OS4 综合修复方法

## 1. 目标与范围

本文和 `OP13HyperOSFix` 工程的目标设备是 **一加 13**。工作目录沿用了
`E:\MIO\ace3`，但本次所有分析、传感器映射和修复逻辑均针对一加 13，不是
一加 Ace 3。

模块解决以下问题：

1. 低功耗 AOD/关屏状态下 F4 单击、双击唤醒。
2. F4 唤醒的距离传感器防误触。
3. 抬起亮屏传感器类型和事件语义不匹配。
4. FOD 移动检测、口袋/NonUI 距离传感器不匹配。
5. 息屏指纹动画缺少可靠的传感器按下/松开事件。
6. 指纹速启延迟、短触后残留速启状态和透明触摸窗。
7. 小米步数服务只查找 wakeup type 18，导致一加步数传感器不可用。
8. 强制开放设置页和服务端需要的能力开关。
9. LHDC V5 编码包间隔仍为 20 ms，导致移植后的 V5 链路不顺畅。
10. 厂商 `appname` 音频参数在移植环境触发卡顿和状态异常。
11. 系统设置的通知图标数量只有 0、1、3，无法选择 2、4、5、6。
12. HyperOS 隐藏频道通知的重要程度、角标、悬浮和锁屏等详细设置。
13. Android 17 包管理服务需要可选的降级、签名和安装策略兼容功能。

模块不依赖 Sense，不替换 `MiuiSystemUI.apk`、`framework.jar` 或
`miui-services.jar`，也不修改内核 keylayout。音频库只做运行时 bind mount，不写入
只读系统分区。实现采用 LSPosed、事件驱动内核桥接和低开销 root 组件。

## 2. 分析依据

主要分析材料：

- OS4 SystemUI：`analysis/os4_systemui/MiuiSystemUI-OS4.0.0.9.apk`
- OS4 SystemUI JADX：`analysis/os4_systemui/jadx-src/sources`
- OS4 SystemUI smali：`analysis/os4_systemui_smali`
- OS4 framework：`analysis/os4_framework`
- OS3 工作模块：`analysis/os3_module_06_extract`
- 当前 OS3 真机成品：`analysis/os3_current_phone/MiuiSystemUI.apk`、
  `analysis/os3_current_phone/Sense.apk`
- OS3 修改前后 smali：`analysis/os3_module_06_smali`、
  `analysis/os3_original_selected`
- OS4 模块回归对照：`analysis/op13fix_v16_jadx`、
  `analysis/op13fix_v17_jadx`
- 原 OS3 修复包：`C:\Users\a1510\Downloads\06_sysuifp_vibrator_mountify.zip`
- Sense 源码：`D:\gongzuoqu\Sense_src_0728`
- 历史记录：`记录.md`、`传感器移动口袋抬起修复方法.md`
- 其他设备报告：`E:\MIO\CustomOSHelper_report.md`
- AOD 点击记录：
  `C:\Users\a1510\Desktop\SystemUI_AOD单双击亮屏修改总结_20260807.md`

旧的 `analysis/op13_aod_pocket_guard_hook` 仅用于验证 Gradle/LSPosed 工程结构。
它依赖 Sense 单击广播的业务逻辑没有进入本模块。

## 3. 总体结构

### 3.1 LSPosed 作用域

- `android`（新版推荐列表显示为 `system`）：F4 输入策略、独立距离门控、步数服务。
- `com.android.systemui`：抬起、FOD 传感器、指纹动画、速启。
- `com.android.settings`：抬起亮屏能力开关。
- `com.android.bluetooth`：进程内一次性修正 LHDC V5 编码包间隔。
- `com.miui.securitycenter`：手机管家充电协议、电池信息与时间修复。
- `com.miui.securitymanager`：安全服务稳定接口和充电协议兼容。

入口是：

`local.mio.op13hyperosfix.HookEntry`

APK 使用与已验证的 `ColorOSWalletCompat.apk` 一致的 legacy 声明：manifest
`xposedscope` 指向字符串数组，`xposedminversion` 为 93。数组内容为：

```text
android
com.android.systemui
com.android.settings
com.android.bluetooth
com.miui.securitycenter
com.miui.securitymanager
```

Vector 会在界面内部把 legacy 的 `android` 映射为 modern 名称 `system`。没有放置
`META-INF/xposed/java_init.list`、`module.prop` 或孤立的 `scope.list`：这些属于
CustomOSHelper 使用的 modern API 102 模块格式；只复制资源而不把所有 Hook 迁移到
libxposed 会使加载器分类错误。

### 3.2 root 组件

- `f4_wake_bin`：设置双击相关 Settings/prop，并调用
  `/odm/bin/touchHidlTest` 打开触控固件 F4 上报；设置完成后阻塞在 `pause()`。
- `op13_fod_bridge.ko`：订阅一加触控驱动已有的 FOD notifier，将精确 down/up
  映射为专用虚拟输入设备的 F5；无轮询。
- `SysUIFPfix`：仅作为 FOD KO 无法加载时的 `/dev/kmsg` 阻塞式回退，发送
  SystemUI down/up 广播，不操作 HBM。
- `op13_ltpo_restore.ko`：事件驱动恢复一加 13 ADFR 最低帧率，由一次性加载器
  等待系统完整开机后再执行 `insmod`。

安装器将二进制放到：

```text
/data/adb/op13_hyperos_fix/bin/f4_wake_bin
/data/adb/op13_hyperos_fix/bin/SysUIFPfix
/data/adb/op13_hyperos_fix/bin/op13_fod_bridge.ko
/data/adb/op13_hyperos_fix/bin/op13_ltpo_restore.ko
/data/adb/op13_hyperos_fix/bin/op13_ltpo_loader.sh
```

不再创建 `/data/adb/service.d` 脚本。LSPosed 激活后，system_server 在每次开机
`PhoneWindowManager.systemReady()` 完成 12 秒后发送一次静默自举广播，40 秒时再
发送一次最终复查广播。两次都是受 `android.permission.DEVICE_POWER` 保护的显式
广播；第二次执行完便不再保留任务。模块应用进程收到广播后：

1. 通过 `su` 验证实际 uid 是否为 0，不只检查 `su` 文件是否存在。
2. 首次安装或 APK 升级时复制两个二进制、两个 KO 和一次性加载器；平时只补拉
   必需的组件。
3. 用 `/system/bin/setsid` 启动并完全脱离 `su` shell。
4. 若尚未完整开机，后台加载器每秒只读一次 `sys.boot_completed`；变为 `1` 后
   才执行 KO 加载并退出。
5. FOD KO 加载成功时确保 `SysUIFPfix` 退出；KO 缺失、符号不兼容或非 6.6
   内核时才启动 `/dev/kmsg` 回退。
6. 12 秒早期调用未取得 ROOT 时不误报；40 秒最终复查仍缺失、拒绝或超时时才显示
   中文长 Toast。开机 60 秒宽限期内首次打开 UI 也只做一次 4 秒延迟复查。
7. 仅当两份 OS4 音频库原始 SHA-256 完全匹配时，在 PID 1 mount namespace 中
   bind mount 精确补丁并重启一次 `audioserver`；已挂载时只检查哈希，不重复重启。

打开桌面入口也会立即执行同一检测并自动补启动。手动按钮只用于重新授权或强制
部署，不是正常启动的前置步骤。没有 JobScheduler、前台服务、保活心跳或失败
重试循环；上述重试都是开机阶段的有限一次性任务。

## 4. 多版本 OS4 兼容策略

OS4 SystemUI 的核心类没有被重命名：

- `com.miui.keyguard.biometrics.fod.MiuiGxzwManager`
- `com.miui.keyguard.biometrics.fod.MiuiGxzwSensor`
- `com.android.keyguard.injector.KeyguardSensorInjector`

不稳定的是匿名类编号、合成类名和 `$r8$classId`。本模块不绑定
`MiuiGxzwIconView$2`、`MiuiGxzwQuickOpenView$1` 或某个 classId，而是在核心
对象构造完成后读取它持有的 listener/Runnable，再 Hook 该对象的实际运行时
类。OS4 小版本重新 R8 后，只要核心管理器和字段契约保留，Hook 仍可工作。

所有子功能独立安装。某个类、字段或传感器不存在时，仅跳过该子功能并写
`OP13HyperOSFix` 的 Xposed 日志，不主动结束 SystemUI 或 system_server。

LHDC 使用 V5 专属的 ARM64 函数组合与指令掩码定位，不依赖固定文件偏移；要求
未补丁/已补丁特征总计只命中一处，否则拒绝写内存。音频库属于 `audioserver`
原生进程，不能由常规 Java Hook 覆盖，因此使用严格源哈希保护：目前只支持实测的
OS4.0.0.9 两份原库，任何 OTA 或用户自改库均 fail-closed，不做部分挂载。

两个 KO 面向 Android 15 的 6.6 GKI/KMI 环境。FOD KO 只在初始化时通过 kprobe
解析厂商 notifier 注册/注销符号，之后直接接收回调；没有依赖固定地址或
`/dev/input/eventX` 编号。启用 `CONFIG_MODVERSIONS` 且保留相同 GKI KMI/厂商
notifier ABI 的 6.6 自定义内核通常可用，但不能把“同为 6.6”视为无条件 ABI
兼容。加载失败会保持系统原状并启用用户态指纹事件回退。

当前没有把 DexKit 打入 APK。现有命名和对象发现足以覆盖已提取的 OS4，加入
DexKit只会增加体积和启动扫描。若未来版本连核心类名也改变，可在进程初始化时
使用 DexKit按方法字符串和调用关系定位一次，再缓存结果；禁止在触摸、F4 或
SensorEvent 热路径执行扫描。

## 5. F4 单击和双击唤醒

### 5.1 输入识别

一加触控固件上报：

- Linux scan code：`62`
- Android key code：`KEYCODE_F4`，即 `134`

`/dev/input/eventX` 每次启动可能变化，因此模块不依赖 event 节点或 deviceId。
Hook 位置为 system_server：

```text
com.android.server.policy.PhoneWindowManager
    #interceptKeyBeforeQueueing(KeyEvent, int)
```

只处理 `ACTION_DOWN` 且 `repeatCount == 0`，F4 的 down/up 都被输入策略消费，
不会传给前台应用。

### 5.2 280 ms 判定

- 第一次 F4 down：创建一个 280 ms 延时任务。
- 280 ms 内第二次 F4 down：取消单击任务，立即完整唤醒屏幕。
- 280 ms 内没有第二次：由 system_server 直接注入 key code `354` 的 down/up，
  唤醒低功耗 AOD。
- 双击后有 500 ms 冷却，第三次误触不会再产生单击 AOD。

延时任务带实例校验。即使 Handler 繁忙导致旧任务晚执行，旧任务也不能提交
新一轮点击，避免边界竞态。

完整唤醒优先调用 `PhoneWindowManager#wakeUpFromWakeKey(KeyEvent)`，再以
`WindowWakeUpPolicy`/`PowerManager` 作为版本兜底。单击直接调用
`InputManager#injectInputEvent`，触摸路径不会启动 `su`、`input` 或 `am` 子进程。

OS4 第一阶段/高功耗 AOD 的原生双击路径仍由 SystemUI 处理，不复刻手势识别，
只在原方法真正准备唤醒前补充一加距离传感器门控。

### 5.3 独立距离门控

Sense 旧方案从 SystemUI FOD NonUI 状态判断口袋，本模块的 F4 逻辑位于
system_server，不能依赖 SystemUI 或 Sense。

在 `PhoneWindowManager#startedGoingToSleep` 后注册 wakeup type 8 距离传感器，
在 `finishedWakingUp` 后立即注销：

- `0.0`/小于阈值：近，阻止 F4 单击和双击唤醒。
- 远：允许唤醒。
- 尚未收到首个值或传感器不存在：fail-open，防止手机永久无法唤醒。

距离传感器只在非交互屏幕阶段启用，没有轮询，亮屏不占用该传感器。

### 5.4 第一阶段 AOD 双击防误触

入口为：

```text
com.android.systemui.shade.PulsingGestureListener#onDoubleTapEvent()
```

原生 OS4 已检查 `FalsingManager.isProximityNear()` 和
`isFalseDoubleTap()`，这些判定完整保留。模块额外使用一加 13 的 wakeup type 8
距离传感器兜底，原因是移植系统的 FalsingManager 不一定绑定正确硬件：

- `ACTION_SCREEN_OFF` 后注册，`ACTION_SCREEN_ON` 后注销。
- 已收到近距离值时才阻止原方法；未知或缺少传感器时 fail-open。
- 不生成点击、不替换原双击识别，也不影响无遮挡状态下的原生唤醒路径。
- 监听纯事件驱动，无轮询和持续 Handler 任务。

## 6. 抬起亮屏

### 6.1 原因

OS4 `KeyguardSensorInjector` 默认查找：

```text
type 33171036
oem7 Pick Up Gesture
pickup  Wakeup
```

一加 13 实际使用：

```text
type 22
pick_up_motion  Wakeup
```

事件语义也相反：

- 一加 raw `0`：抬起。
- 一加 raw `1`：放下/稳定。
- raw `2`：保留给 MIUI 原有放下分支。

MIUI 监听器期望 `1` 为抬起、`0/2` 为放下。

### 6.2 修复

模块替换 `updatePickupSensorRegistration()` 的传感器注册部分：

- 使用 wakeup type 22。
- 沿用 SystemUI 原本的 `mPickupSensorListener`、Handler 和生命周期条件。
- 传给原监听器前转换 `0 -> 1`、`1 -> 0`，`2` 不变。
- 传感器只在原 MIUI 逻辑要求的非交互/放下检测阶段注册。

同时强制：

```text
FeatureParser: support_gesture_wakeup = true
Settings.System: pick_up_gesture_wakeup_mode = 1
```

Settings 进程也开放 `isSupportPickupWakeup` 能力，避免设置页隐藏或关闭入口。

## 7. FOD 移动和口袋/NonUI

### 7.1 移动检测

MIUI 原传感器：`33171030`。

一加 13 实际传感器：`33171026`，wakeup。

语义映射：

- 一加 raw `2`：移动，转换为 MIUI `1`。
- 一加 raw `1`：稳定，转换为 MIUI `2`。
- `3` 保持不变。

提取的 OS4 原生 `MiuiGxzwSensor` 没有跑步/步态分类器。官方现有策略是：移动时
显示 FOD 图标并重置 5 秒透明计时，稳定且计时已到时隐藏，距离遮挡进入 NonUI
时立即隐藏。公开的小米技术资料和开源代码中也没有找到额外的跑步抑制策略。
因此模块只校正传感器 ID 和事件语义，不新增惯性延时、采样窗口或常驻运动分析；
跑步时持续判为移动属于原逻辑的边界行为，避免为此破坏抬起和正常 FOD 可发现性。

### 7.2 FOD NonUI/口袋

MIUI 原私有传感器：`33171027`。

一加 13 使用 wakeup type 8 距离传感器：

- raw `0.0`：近/遮挡，转换成 MIUI `1`，进入 NonUI。
- 非零（实测远值 `5.0`）：转换成 MIUI `0`，退出 NonUI。

模块根据 wakeup type 8 是否存在强制设置 `mSupportNonuiSensor`，不再依赖小米
触控属性、资源布尔值或私有传感器 ID。

正确传感器仍由 `registerDozeSensor()`/`unregisterDozeSensor()` 的原生命周期
控制，只在 FOD doze 阶段工作。注册任务带 generation 校验，快速亮灭屏时旧的
异步注册不能越过新的注销请求。

## 8. 指纹事件和动画

### 8.1 内核快速路径

一加触控驱动已经通过 blocking notifier chain 发布 FOD 专用事件。本模块的
`op13_fod_bridge.ko` 注册：

```text
touchpanel_event_register_notifier
touchpanel_event_unregister_notifier
action = EVENT_ACTION_FOR_FINGERPRINT (1)
touch_state = 1/0 (down/up)
```

KO 创建名称严格为 `op13_fod_bridge` 的虚拟输入设备，将状态映射为 Linux
`KEY_F5`（scan 63）。event 编号由 Linux 动态分配，代码不保存也不扫描固定的
`event12`。notifier 回调只执行状态去重、`input_report_key()` 和 `input_sync()`，
没有工作队列、日志热路径或轮询。

system_server 的输入策略同时校验 scan 63、Android `KEYCODE_F5` 和设备名
`op13_fod_bridge`，因此外接键盘的 F5 不会被误当成指纹。匹配后直接向
`com.android.systemui` 发送受 `DEVICE_POWER` 保护的 down/up 广播并消费按键。
热路径不经过 `su`、`am`、正则或日志读取。

### 8.2 用户态回退

只有 FOD KO 未加载时才运行 `SysUIFPfix`。它从 `/dev/kmsg` 当前末尾开始读取：

- `fingerprint down`：发送一次 SystemUI down 广播。
- `fingerprint up` 或 600 ms 超时：发送一次 SystemUI up 广播。

进程使用 `epoll_wait()` 阻塞，不使用 `logcat` 管道、正则或空转读取。回退路径
需要通过 `am broadcast` 创建短生命周期子进程，因此延迟和瞬时开销均高于 KO
快速路径，但日常空闲不占用 CPU。

### 8.3 SystemUI 接收

`MiuiGxzwManager` 构造后动态注册 exported receiver，优先要求发送者拥有
`android.permission.DEVICE_POWER`。root uid 可发送；如果某版本拒绝该注册形式，
再回退到 exported receiver。

当前 OS3 真机的 Sense 1.12 会阻塞读取 HAL `UffMessageQueue` 日志，将
`what:2004/2005` 转成 down/up。OS3 SystemUI 收到 down 后执行：

```text
onFpsPointerUp()
onFpsPointerDown()
isCatchDownEvent = true
```

该顺序是 OS3 缺失触摸桥接时的完整替代链路，不能原样用于 OS4。OS4 自身已经
处理真实 FOD MotionEvent；模块 v1.7-v1.9 每次 down 都强制 `Up -> Down`，会与
原生事件重复调用 `onTouchUp(true)`、高亮透明度和动画状态机，实机结果是锁屏
指纹图标/底部图标变黑，严重时指纹动画消失。回退 v1.6 后现象立即消失。

v1.10 恢复 v1.6 的条件式补偿：

```text
down:
  记录 externalDown
  仅当 mTouchDown=false 时调用 onFpsPointerDown，并标记 syntheticDown
  isCatchDownEvent=true

up:
  清 externalDown
  若整段屏幕手势仍按着，则交给原生 MotionEvent 继续处理
  否则仅释放 syntheticDown，并清理尚未显示的速启任务
```

因此模块不会主动 Up 掉 OS4 原生建立的触摸状态，也不会在每次 down 重置图标和
高亮状态。`com.oplus.fp.ANIM_DOWN` 只作为旧二进制的 down-only 兼容入口；KO
快速路径和 `SysUIFPfix` 回退均使用有配对语义的
`com.sysuifpfix.action.FINGERPRINT_DOWN/UP`。

### 8.4 OS4 HBM 结论

澎湃 OS4 的一加显示/指纹驱动会在认证和 AOD 状态转换中自行处理 HBM。实机验证
表明额外写 HBM 反而可能令低功耗 AOD 指纹后黑屏。最终版本没有 HBM 节点、没有
HBM shell 命令，也不会在 down/up 时补写亮度；该职责完全交回厂商驱动。

## 9. 指纹速启

### 9.1 统一延迟

OS4 原始逻辑按桌面/非桌面使用 500 ms/1000 ms。模块在
`MiuiGxzwManager#dismissGxzwView()` 原逻辑完成后，只在 `mShowRunnable` 确实
处于 Handler 队列时取消并改为统一 `450 ms`。该 450 ms 从 dismiss 完成时开始，
和当前 OS3 真机成品中的两个 `0x1c2` 常量一致；不会扣除认证前已经按住的时间，
避免快速认证时在 SystemUI 状态切换完成前过早显示。

不 Hook 全局 `Handler#postDelayed`，不会影响 SystemUI 其他任务。

后续仍由原代码检查 `fod_quick_open`、指纹解锁结果、编辑模式和设备初始化状态，
模块不在 dismiss 前篡改字段，也不强行绕过用户开关。

### 9.2 短触残留问题

息屏短触会由 FOD 事件桥接补充 down。手指已经离开时，MIUI 仍可能保留
`mTouchDown`/`mShowed`，解锁后延迟出现速启 UI，甚至只留下一个透明的
`gxzw_touch` 窗口遮挡屏幕。

不能无条件在 fingerprint up 时删除速启 runnable，否则手指从指纹区域横向滑向
快捷项时，FOD notifier 会先报 up，正常速启也会被打断。只有屏幕手势也已经结束
时才清理待显示任务，最终是否显示仍由屏幕触摸门控决定。

OS4 的 `MiuiGxzwIconView` 同时持有两个相同匿名类实例：
`mKeyguardUpdateMonitorCallback` 和 `listener`。只有后者注册进
`MiuiGestureMonitor`。v1.6/v1.7 按字段名取到前者，虽然 Hook 了正确的方法类，
运行时实例却不在映射表中，导致触摸状态始终不更新。v1.10 枚举 IconView 的实例
字段，找出所有具有 `onPointerEvent(MotionEvent)` 的对象；同一 Method 只 Hook
一次，但两个实例都建立到所属 IconView 的映射。

真实 pointer listener 提供整段全屏 MotionEvent：

- ACTION_DOWN：记录本次屏幕手势仍按着。
- MOVE/ACTION_POINTER_UP（包括滑出指纹圆圈）：保持按着，不重新按 FOD 圆圈判定。
- ACTION_UP/ACTION_CANCEL：记录手势结束。

`mShowRunnable` 执行前再次检查 OS3 同语义的整屏触摸状态：

- 仍按住：运行原速启 UI。
- 已松开：调用 `MiuiGxzwQuickOpenView.dismiss(1)` 并跳过 show runnable。

必须调用 `dismiss(1)`，不能只清 `mShowed` 或 `mFingerID`，否则 WindowManager
中的透明触摸窗不会被移除。

## 10. 步数传感器

OS4 `com.miui.server.stepcounter.StepCounterManagerService` 原代码：

```java
mSensorManager.getDefaultSensor(18, true)
```

一加 13 暴露的是标准 type 18 **non-wakeup** 步数检测传感器。模块在服务构造
完成后设置：

```java
mSensorManager.getDefaultSensor(18, false)
```

并强制 `FeatureParser.getBoolean("support_steps_provider") = true`。服务后续的
200000 us 采样和 300000000 us batching 仍由原 MIUI 代码管理，没有新增轮询。

## 11. LHDC V5

### 11.1 OS3 旧方案复核

`C:\Users\a1510\Downloads\16_lhdc_dynamic.zip` 是 OS3 最终版。与一加 13 OS3
原库逐字节对比后，它只改了 4 条指令：两处提前 `RET` 禁用 v2/v3 dump，两处把
V5 `fopen()` 调用改成返回空指针，从而不再生成：

```text
/sdcard/Download/lhdcv5.raw
/sdcard/Download/sourcev5.pcm
```

OS4 原始 `libbluetooth_jni.so` 中已经没有这两个路径，不需要移植 dump 补丁。
OS4 的 `A2DP_GetPacketTimestamp` 也已经识别 codec ID `0x4c35053aff`，并调用 V5
专用时间戳解析函数。因此不能再套用旧版“未知 codec 分支 NOP”补丁，否则会扩大
影响范围。

### 11.2 OS4 实现

OS4 唯一需要修正的是：

```text
a2dp_vendor_lhdcv5_get_encoder_interval_ms
mov w0, #20  ->  mov w0, #10
```

`com.android.bluetooth` 的 `AdapterService` 创建时加载 APK 内的 ARM64 helper。
helper 通过 `dl_iterate_phdr()` 查找已加载的 `libbluetooth_jni.so`，扫描可执行段并
同时验证：

- cleanup/reset/flush/interval/frame-size 的共享函数模板；
- V5 后方独有的 `BTI; ADRP x8; STR w0,[x8,#imm]; RET` 状态写入函数；
- 当前间隔指令只能是 20 ms 或已经补过的 10 ms；
- 整个进程只能有一个符合 V5 语义的目标。

写入前临时给目标代码页增加写权限，原子替换 4 字节并清理指令缓存，随后恢复
RX。成功后缓存状态，不再扫描。实测 OS4.0.0.9 目标文件偏移为 `0x9FB4CC`，但
运行时代码不使用该固定偏移。

## 12. 音频卡顿

### 12.1 进程边界

`libaudiopolicymanagerimpl.so` 和 `libmiaudiopolicymanager.so` 由独立 native
`audioserver` 加载，不在 zygote/LSPosed Java 进程中。直接用 Java Hook 屏蔽异常
不可行；注入 `audioserver` 的风险也高于两份受哈希保护的 bind mount。

### 12.2 精确补丁

旧 OS3 整函数/宽范围补丁会破坏 FadeIn、动态音频用例或清理状态，极少数路径会
让 SystemUI 音量面板拿到不完整 `StreamState`，表现为 `StreamState.level` 空指针。
OS4 最终方案保留所有业务逻辑，只把 7 个向 HAL 写入 `appname=+/-package` 的
`blr x8` 改为 `nop`：

```text
libaudiopolicymanagerimpl.so: 0x3DCBC, 0x41424
libmiaudiopolicymanager.so:   0x1A39C, 0x1A48C, 0x1A548, 0x1A6F8, 0x1A77C
00 01 3F D6 -> 1F 20 03 D5
```

源库 SHA-256：

```text
libaudiopolicymanagerimpl.so E4D9605A588ADE5C9DAD5171CD38E1A76220CEC9FA61DE785592F8ED98ED2081
libmiaudiopolicymanager.so   8DF1337CF23B7A350441BC8BAF698F1BD503AF2B8CCEE3506E7165DE870A518C
```

补丁库 SHA-256：

```text
libaudiopolicymanagerimpl.so D3D5E046FEE68C9F892550890E85423DC3E5B4E288890AE8D66BC58B75D706A1
libmiaudiopolicymanager.so   BDBB8CF00276628000DB0DD66D7E4837775DB028C96D33B2C483DE49D79F859C
```

两份源文件以不可变哈希后缀保存到 `/data/adb/op13_hyperos_fix/audio`，权限为
`0644 root:root`，标签为 `u:object_r:system_lib_file:s0`。挂载和校验都通过
`nsenter -t 1 -m` 执行。第二份挂载或最终校验失败时会卸载第一份并回滚，禁止
半套补丁运行。OTA 后源哈希变化时显示 `incompatible` 并保持系统原库。

补丁生成脚本：`analysis/patch_os4_audio.ps1`。它同时校验整库 SHA-256 与每个
调用点的原始 4 字节，输出差异必须严格为 7 条指令。

## 13. 空闲功耗设计

本模块不能宣称物理意义上的绝对 0 功耗，但已经将新增空闲 CPU 开销压到事件
驱动可达到的最低水平：

- F4：只在按键到达时执行；单击判定只有一个 280 ms Handler 任务。
- F4 距离门控：只在屏幕非交互阶段注册 wakeup sensor，亮屏注销。
- 第一阶段 AOD 防误触：同样只在息屏阶段监听 wakeup type 8，亮屏注销。
- 抬起/FOD/步数：复用 Sensor HAL batching 和原系统生命周期。
- `f4_wake_bin`：配置完成后 `pause()`，没有循环轮询。
- `op13_fod_bridge.ko`：直接接收厂商 notifier；每次指纹只报告一次 F5 down/up。
- `SysUIFPfix`：仅 KO 失败时存在，阻塞在 `epoll_wait()`；KO 正常时进程不存在。
- 速启：只 Hook 原有 MotionEvent 和一个 show runnable，不创建定时扫描。
- 开机：system_server 只发送一次显式广播，应用完成 `su` 检测和拉起后即可退出。
- LTPO：只在开机未完成时每秒检查一次属性；成功 `insmod` 或发现已加载后进程
  立即退出，日常没有常驻线程。
- 触摸热路径没有 DexKit、正则、root shell、`input` 或 `am` 子进程。
- LHDC：只在 Bluetooth 服务启动时扫描一次本进程代码段，成功后没有线程、定时器
  或轮询。
- 音频：开机/打开应用时校验两次 SHA-256；已挂载后不重启服务，日常播放路径只
  少执行 7 个 HAL 参数调用，不新增后台任务。

正常快速路径相对原生 DSP 方案只增加一次 notifier 回调、两次 input 上报和进程内
广播分发，空闲时没有持续 CPU 空转。只有 KO 不兼容时才退回 `/dev/kmsg + am`。

## 14. 工程与构建

工程：

```text
E:\MIO\ace3\OP13HyperOSFix
```

Java：

```text
E:\MIO\ace3\tools\temurin17
```

Gradle：本机缓存的 9.3.1；AGP 9.1.1；compileSdk/targetSdk 37；Java 17。LHDC helper 使用
官方 Android NDK r28c（`28.2.13676358`）和 arm64-v8a，源码位于
`app/src/main/cpp`。

当前本地产物：

```text
OP13HyperOSFix-v1.35-md3-haptics.apk
SHA-256: 4BEE92868A9C1E407ED544EF44AF3DF017311886ADCA6623C2484DAAA6C3D5D7
```

构建命令：

```powershell
$env:JAVA_HOME='E:\MIO\ace3\tools\temurin17'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
& 'C:\Users\a1510\.gradle\wrapper\dists\gradle-9.3.1-bin\23ovyewtku6u96viwx3xl3oks\gradle-9.3.1\bin\gradle.bat' :app:assembleRelease :app:lintVitalRelease --no-daemon
```

`SysUIFPfix` 可复现编译：

```bash
aarch64-linux-gnu-gcc -std=gnu17 -O2 -pipe -Wall -Wextra -Werror \
  -static-pie -Wl,--gc-sections -s \
  -o app/src/main/assets/SysUIFPfix native-src/SysUIFPfix.c
```

native 源码位于 `native-src`。APK 中 `f4_wake_bin` 使用 Sense 0728 的最新成品。
`op13_ltpo_restore.ko` 与 `07_ltpo.zip` 中的文件 SHA-256 完全一致：

```text
CD47CD1DB4EABE31E73618BBAB08DFB2260CAAACB0442F1386B1ECE865D4061B
```

启动图标使用 `res/drawable-nodpi/app_icon_24.png` 的紫金像素风 `24`，Android 8+
通过 adaptive icon 资源加载，旧式启动器使用同一图像回退。

## 15. 安装步骤

1. 安装 release APK。
2. 在 LSPosed 启用模块，作用域选择：Android 系统、系统界面、设置、蓝牙、手机管家、
   安全服务。APK 已声明这六个推荐作用域。
3. 打开模块应用并授予 ROOT；应用会检测 ROOT、配置 F4 并安排两个 KO 加载。
4. 确认页面显示“ROOT：已授权”、“指纹事件组件：内核桥接”；系统完整开机后
   `LTPO 内核模块` 应显示“已加载”，`OS4 audio fix` 应显示 `active`。若显示
   “日志回退”，说明 FOD KO 未加载；若音频显示 `incompatible`，不要强制挂载。
5. 重启手机，使 system_server 和 SystemUI Hook 从启动阶段生效。以后每次启动由
   LSPosed 的 system_server Hook 自动拉起 native 组件，不依赖 KernelSU
   `service.d`。

若开机或打开应用时未取得 ROOT，页面会显示失败原因，并弹出中文长 Toast。应用
保留桌面入口和“授权 ROOT 并重新启动”按钮，便于 ROOT 管理器首次授权或拒绝后
重试。

这里签名的是独立 LSPosed 应用，必须有 APK v2 签名才能安装；没有修改或重签
系统的 `MiuiSystemUI.apk`。

## 16. SELinux 要求

root 组件需要：

- 执行 `/odm/bin/touchHidlTest` 和 `settings`。
- FOD KO 失败时读取 `/dev/kmsg` 并执行 `/system/bin/am broadcast`。
- 通过 root shell 执行 `insmod`。
- 读取、复制并 bind mount 两份音频库，以及重启 `audioserver`。

不要使用宽泛 permissive 规则。首次运行后只根据实际 AVC 补对应 domain 到这些
具体节点/文件的最小权限。正常 FOD KO 快速路径不需要 `/dev/kmsg` 权限；若回退
日志报 `cannot open /dev/kmsg`，优先检查 SELinux 和节点标签。

## 17. 日志与排错

LSPosed 日志过滤：

```text
OP13HyperOSFix
```

root 日志：

```text
/data/adb/op13_hyperos_fix/log/f4_wake.log
/data/adb/op13_hyperos_fix/log/SysUIFPfix.log
/data/adb/op13_hyperos_fix/log/op13_ltpo_loader.log
```

进程：

```sh
pidof f4_wake_bin
pidof SysUIFPfix
grep '^op13_fod_bridge ' /proc/modules
grep '^op13_ltpo_restore ' /proc/modules
```

`op13_fod_bridge` 已加载时，`pidof SysUIFPfix` 没有输出是正确状态，不是组件丢失。

音频挂载校验：

```sh
nsenter -t 1 -m -- sha256sum \
  /system_ext/lib64/libaudiopolicymanagerimpl.so \
  /system_ext/lib64/libmiaudiopolicymanager.so
```

LHDC 成功日志应为 `LHDC V5 packet interval patched: 20 ms -> 10 ms`。`status=-2`
表示特征不唯一或版本不兼容，helper 未写入目标进程。

模块按子功能输出一次初始化结果。不要在正常运行时开启持续 debug 轮询。

## 18. 测试矩阵

### F4/AOD

- 低功耗 AOD 单击：约 280 ms 后进入 AOD。
- 低功耗 AOD 双击：第二次 down 立即完整亮屏。
- 完全关屏单击/双击。
- 三连击和 280 ms 边界点击。
- 遮挡顶部距离传感器后单击/双击不唤醒。
- 第一阶段/高功耗 AOD 遮挡时原生双击不唤醒，无遮挡时行为不变。
- 亮屏后确认距离传感器监听已注销。

### LTPO

- `sys.boot_completed=0` 时确认没有执行 `insmod`。
- `sys.boot_completed=1` 后只加载一次，参数为
  `min_fps=1 delay_ms=350 apply_on_load=1`。
- 重复打开应用不重复加载，日志无 `File exists` 或重复 hook。

### 抬起和步数

- 关屏抬起亮屏。
- 抬起后放下的原 MIUI 语义。
- 设置页开关可见且保持开启。
- 重启后步数服务使用 non-wakeup type 18 并持续累计。

### 指纹/FOD

- AOD、完全关屏、低亮度下按指纹，动画正常且不会黑屏。
- 快速按下/松开、连续点击、长按。
- 遮挡距离传感器时 FOD 进入 NonUI，远离后退出。
- 移动/稳定时指纹图标显示语义正确。

### 指纹速启

- 锁屏亮屏状态按住解锁，450 ms 后可滑向快捷项。
- AOD/关屏状态按住解锁，速启同样可用。
- AOD/关屏快速碰一下就松开，解锁后不出现速启 UI。
- 短触后屏幕不留透明遮挡区域。
- 正常速启出现后滑动不会被 fingerprint up 全局取消。

### LHDC 与音频

- Bluetooth 进程重启后确认 V5 特征只命中一处，内存指令为 `40 01 80 52`。
- 蓝牙保持 ON、A2DP 服务正常，连接 V5 耳机实测播放、切歌、暂停和重连。
- `/sdcard/Download` 不生成 `lhdcv5.raw`、`sourcev5.pcm`。
- 首次应用音频补丁时 `audioserver` 只重启一次，再次打开应用 PID 不变。
- 音乐、通话、录音、音量面板、应用切换和前后台切换无卡顿或空指针。
- OTA/自改库哈希不匹配时不挂载任何一份音频库。

## 19. 当前验证状态

离线已完成：

- OS4/OS3 代码交叉核对。
- ARM64 `SysUIFPfix` static-PIE 编译和 `--version` 检查。
- Gradle `clean assembleDebug` 和 `lintDebug`。
- Gradle `clean assembleRelease` 和 `lintRelease`（0 error）。
- APK manifest、Xposed 元数据、作用域和 assets 检查。
- APK v2 签名验证。
- LSPosed system_server 单次自启、应用打开自动 ROOT 检测及无 `service.d` 路径的
  静态复核。
- F4 延时任务边界竞态静态复核。
- `PulsingGestureListener` 原生防误触路径和 type 8 兜底生命周期静态复核。
- LTPO KO 与 `07_ltpo.zip` 哈希核对、`modinfo` 参数核对、加载器 shell 语法和
  Toybox `flock` 文件描述符行为验证。
- 512x512 像素图标、adaptive/legacy 资源和 APK manifest 检查。

真机 OS4.0.0.9 已完成：FOD/LTPO KO 加载、FOD notifier down/up 精度、专用虚拟
输入设备、system_server/SystemUI Hook 初始化、ROOT 自部署和 KO 成功后停止
`SysUIFPfix` 的验证。随后确认 v1.7-v1.9 的无条件 `Up -> Down` 是 OS4 黑图标和
动画消失的回归源，v1.6 为无该回归的基线；当前版本保留 v1.6 条件式动画补偿、
双 pointer 实例映射、整段屏幕手势门控和 dismiss 后固定 450 ms。

v1.13 已安装到 OS4.0.0.9 真机并完成以下新增验证：

- 音频原库哈希匹配后，两份补丁库在 PID 1 namespace 中挂载成功；目标与源文件
  哈希、`0644 root:root` 和 `system_lib_file` 标签均正确。
- 首次挂载令 `audioserver` 从 PID 2523 重启为 17921；重复打开应用后仍为 17921。
- Bluetooth 作用域已加入 Vector，进程初始化日志返回 LHDC 状态 `1`。
- 进程内 `0x9FB4CC` 对应指令实读为 `40 01 80 52`，Bluetooth 保持 ON，crash
  buffer 没有 Bluetooth 崩溃。
- release `lintRelease`、APK v2 签名、JNI 导出符号和三份原生资产哈希均通过。

尚未完成的是连接真实 LHDC V5 耳机后的长时间播放、重连、通话/录音与音量面板
人工回归；这些项目仍需按第 18 节执行。模块的兼容失败策略是跳过单项并记录日志，
但 system_server Hook 和 root 音频挂载仍属于高风险位置；应保留可从
recovery/KernelSU 管理器禁用模块的退路。

## 20. MiCharge 与手机管家多版本兼容

综合模块从 v1.24 起吸收 `micharge_lsposed` 的充电协议、充电小数、状态栏/超级岛、
`IMiCharge` 和手机管家电池详情适配。新增推荐作用域：

```text
com.miui.securitycenter
com.miui.securitymanager
```

手机管家整体经过 R8，内部辅助类和字段名会随版本变化。模块不 Hook 这些混淆成员，
也不再硬编码 `ChargeProtectFragment`：在 AndroidX Fragment 生命周期中先过滤
Preference Fragment，再根据电池信息分类及健康、温度、充电次数、循环、生产日期、
首次使用日期等 Preference 键的组合识别目标页。离开页面即停止一秒采样线程，其他
页面没有轮询。

已离线核对以下版本：

```text
HyperOS 3: SecurityCenter 12.2.8 / 12.6.5, SecurityManager 18.0
HyperOS 4: SecurityCenter 13.2.0, SecurityManager 20.0
```

三个手机管家版本的 Fragment 父类和混淆字段不同，但上述 Preference 结构一致。
安全服务作用域不依赖其混淆业务类，只 Hook boot class path 中稳定的
`miui.util.IMiCharge`。`IMiCharge`、MIUI `TextPreference` 或
`PreferenceGroup` 缺失时均按单项降级，不能阻断其余充电 Hook 或电池页直读节点。

目前不引入 DexKit。现有稳定结构足以覆盖已验证版本，运行时扫描 DEX 只会增加首次
加载成本和依赖体积。若后续版本连 Preference 键也改变，再按 DexKit 官方建议用
常量字符串、参数签名和调用关系定位，并按 APK versionCode 缓存 descriptor，扫描
结束立即关闭 bridge：<https://luckypray.org/DexKit/en/guide/home>。

## 21. 手机管家可用时长与预计充满时间

OS4 移植后的两个错误来自手机管家内部的不同数据源：

- 预计充满时间仍使用小米充电历史和默认充电器参数。一加历史无法满足其采样条件时，
  `BatteryChargeTimeHelper` 得到 0，随后被强制钳制为 `60000 ms`，界面因此固定显示
  `0 时 1 分`。
- 使用时长调用 `BatteryStats.computeBatteryRealtime(..., STATS_SINCE_CHARGED)`。
  移植后的统计时基会在重启后只返回本次开机时间，但电量曲线仍保留更早的历史，造成
  曲线横跨数小时而标题只有十几或二十分钟。

修复实现在 `SecurityCenterTimeHooks.java`，只装入手机管家主进程，不修改 SystemUI、
framework、系统全局 BatteryStats 返回值或任何系统 APK：

1. 充电时优先读取 `/sys/class/power_supply/battery/time_to_full_now` 和
   `time_to_full_avg`；放电时优先读取 `time_to_empty_avg`。节点单位为秒，写回手机
   管家时转换为毫秒。
2. ColorOS 16.10 基带会用 `0` 和固定 `3600` 秒表示“不支持估算”。v1.51 明确拒绝
   这些占位值，避免页面固定为充电 `0 时 1 分`、放电 `1 时 0 分`。
3. 节点无效时，以手机管家已加载的 `HistoryData` 连续充/放电段计算电量变化速率；
   样本不足时再依次使用 Android BatteryManager/BatteryStats 和电量、电流节点作保守
   兜底。估算限制在 5 分钟至 30 天，异常值不覆盖原结果。
4. 充电详情、放电可用时长和低于 20% 时的剩余待机提示都调用同一个稳定 helper，
   因此不需要分别 Hook 界面或硬编码文案。
5. Hook 保留手机管家原有排版、文案和数值动画。节点无权限、无效或类结构不匹配时
   单项退出并沿用原值。
6. `PowerLevelBean.setUsageRealTime(long)` 是 `@Keep` 的稳定入口。设置值之前读取它
   已装入的 `HistoryData` 列表，以当前连续历史段的首尾时差作为使用时长下限；原统计
   更长时保持原值，只有重启后统计明显缩短时才校正。手机管家该字段单位为微秒。

此功能没有服务、定时器、广播接收器、root shell 或日志读取。sysfs 仅在手机管家原本
重新计算顶部时间时读取；电量历史只在页面模型提交数据时遍历一次，退出页面后没有新增
CPU 或唤醒开销。

OS4.0.0.9、SecurityCenter 13.2.0 真机验证结果：

```text
预计充满时间：60000 ms -> 23188000 ms（界面 0 时 1 分 -> 6 时 26 分）
使用时长：1248689000 us -> 33167455000 us（界面 0 时 20 分 -> 9 时 12 分）
```

对应产物为 `OP13HyperOSFix-v1.25-security-battery-time.apk`，版本号 26。安装更新后只需
结束并重新打开手机管家进程即可验证；正常开机使用时由 LSPosed 自动加载。

## 22. 通知图标数量 0 至 6

Android 17 设置页 `com.android.settings.IconDisplayCustomizationSettings` 的
`setupShowNotificationIconCount()` 原本只创建 `{0, 1, 3}`，但
`updateShowNotificationIconCount()` 和 SystemUI
`NotificationIconObserver` 都能直接处理任意整数，没有 3 的上限。因此只在设置
进程完成初始化后替换下拉项与 value：

```text
不显示、显示1个、显示2个、显示3个、显示4个、显示5个、显示6个
value: 0, 1, 2, 3, 4, 5, 6
Settings.System: status_bar_show_notification_icon
```

文案复用设置 APK 自己的格式化字符串，不注入资源 ID，不修改 SystemUI 计数逻辑。
实机已确认菜单完整显示，并选择 `4` 后成功写入系统值；测试结束恢复为用户原值 `1`。

## 23. 更多通知设置项

此功能基于 HyperCeiler `MoreNotificationSettings`，并针对 OS4/API 37 的实际 smali
调整。设置进程 Hook：

- 两套 `BaseNotificationSettings#setPrefVisible`：恢复 `importance`、`badge`、OS4 的
  `setting_badge` 和 `allow_keyguard`。
- 同时适配 `com.android.settings.notification.ChannelNotificationSettings` 和 Android
  17 实际使用的 `com.android.settings.notification.app.ChannelNotificationSettings`。
- 在 `setupChannelDefaultPrefs` 中绑定重要程度控件，并从 MIUIX 真正执行选择的
  `Preference#callChangeListener` 写回 `NotificationChannel`。写入后立即通过
  `NotificationManagerServiceCompat#getNotificationChannelForPackage` 重新读取校验，
  不再把下拉框自身的临时变化误判为成功。

SystemUI 新版已没有旧的 `MiuiNotificationMenuRow#onClickInfoItem`。模块同时保留旧
方法兜底，并在 API 37 的 `createMenuViews()` 完成后替换当前 `mInfoItem` 点击监听；
频道从 `NotificationEntry.mRanking#getChannel()` 获取。混合通知和
`miscellaneous` 频道保持系统原行为。

HyperCeiler 还会在
`StackCoordinator$attach$1#onAfterRenderList(List)` 中删除
`Ranking.getImportance() <= 1` 的条目，只让 `importance > 1` 进入后续状态统计。
此处位于通知行渲染完成之后，这是有意的：低重要程度通知仍保留在下拉控制中心，
但不悬浮，也不进入状态栏通知图标相关状态。不要把过滤前移到
`NodeSpecBuilder#buildNodeSpec()`；那会直接删除控制中心里的通知行，属于错误行为。

本机设置资源的四档映射为：紧急 4、高 3、中 2、低 1。写回时设置
`NotificationChannel.USER_LOCKED_IMPORTANCE`，不建立额外频道标记，不改写前台服务
的系统限制，也没有轮询、定时器或后台线程。部分固定系统/前台服务频道若被
NotificationManager 强制抬高重要程度，保持 Android 自身结果。

实机验证频道页可显示桌面角标、四档重要程度、悬浮通知、锁屏通知、声音、振动和
锁屏内容；从通知面板长按通知也能直达同一频道页。开关写入
`Settings.Global.op13_fix_more_notification_settings`，默认开启，修改后重新打开设置
页即可生效，不需要持续后台服务。

## 24. Android 17 包管理服务/核心破解

核心破解代码迁移自 HyperCeiler 当前源码，并逐一用本机 API 37 的
`services.jar`、`framework.jar` 和 `miui-services.jar` smali 核对真实类和方法。
当前涵盖：

- 降级安装、包管理签名验证、最低签名方案、APK 签名比较。
- sharedUserId、split APK 精确签名、沿用已安装签名和安装验证代理。
- 低目标 API、持久应用替换、隔离检查和小米系统应用更新限制。
- 移植系统开启核心破解时的指纹配置保护。

所有核心选项默认关闭。`system_server` 在 `PhoneWindowManager.systemReady()` 时读取
一次 `Settings.Global`；没有任何选项开启时直接返回，不安装包管理 Hook，也没有
观察器、轮询或常驻线程。选项变更必须重启手机。主开关只控制签名核心组，扩展安装
策略和指纹保护按各自开关独立生效；UI 会隐藏或禁用不满足依赖关系的子项。

这些选项会主动降低 Android 安全边界，不应为“以后可能用到”而全开。当前真机只
验证了 API 37 方法签名和默认关闭路径，没有用生产系统包执行不同签名覆盖安装。

## 25. 配置界面、激活检测与许可

应用界面采用接近 HyperCeiler/MIUI 设置页的分组结构，支持深浅色、系统栏 Insets、
8 dp 卡片、图标化行、状态标签和依赖开关。主页显示 LSPosed、ROOT 和原生组件状态，
不要求先点击“安装并启用 ROOT 组件”：正常开机由已有显式启动链自动执行，按钮只供
首次授权和故障重试。

HyperOS 在 `systemReady()` 后才递增 `Settings.Global.BOOT_COUNT`。system_server
写入的激活标记可能比最终计数小 1，因此检测同时接受当前值与前一值，并要求记录的
API 等于当前 `SDK_INT`；下一次未加载模块的启动会相差 2，从而仍能正确报未激活。

通知设置和核心破解的适配代码来自 HyperCeiler，项目根目录包含 `LICENSE` 和
`THIRD_PARTY_NOTICES.md`，记录上游仓库、版本和 AGPL-3.0 许可。各进程的功能入口
分别捕获异常，一个旧功能适配失败不会阻止通知、传感器或其他独立 Hook 安装。

## 26. API 37 新增实机验证

在一加 13、HyperOS `OS4.0.0.9.XPCCNXM`、Android 17/API 37 上完成：

- LSPosed system_server 激活标记写入，主页正确显示“LSPosed 已激活”。
- 核心破解全部为 `0` 时日志确认不安装包管理补丁。
- 设置进程同时命中旧版和 Android 17 `.notification.app` 两套频道页；实际页面日志
  确认为 `com.android.settings.notification.app.ChannelNotificationSettings`。
- 通知图标菜单 0 至 6 完整显示，扩展值能写入并恢复原值。
- 更多通知频道页和四档重要程度下拉正常，通知面板入口可达。以非固定频道
  `com.xiaomi.aicr/aicr_service_notification` 实测从等级 3 写入等级 1，服务端频道
  定义、有效通知等级和用户锁定位均即时更新；验证后已恢复原等级 3。
- SystemUI 的 `StackCoordinator$attach$1#onAfterRenderList` Hook 命中 1 个目标方法；
  未安装 `NodeSpecBuilder` 可见行过滤。
- 低重要程度行为保持 HyperCeiler 语义：不悬浮、不显示状态栏通知图标，但控制中心
  仍保留通知行。
- UI 开关通过一次 `su -c settings put global` 成功写入 `0/1`，测试后恢复开启。
- Release `assembleRelease`、lint vital、资源优化和 APK 打包通过。

日志中另有 `com.miui.securitycenter.remote` 在 Application 创建阶段解析错误的
`networkSecurityConfig` 资源而循环崩溃；堆栈发生在模块业务 Hook 之前，属于当前
移植包自身的资源错位，不是本次通知或核心破解代码产生。

## 27. MD3 界面、SN、LTPO 终端与综合诊断

v1.34 将配置应用改为 AndroidX Compose Material 3，并使用系统动态色、标准 Switch、
按钮和不超过 8 dp 的圆角。主页提供默认开启的低功耗双击亮屏开关，其余自动修复按
功能分格只读展示。关闭双击亮屏时会立即结束 `f4_wake_bin`，重新开启时按需拉起；
事件路径只在 F4 按下时读取一次配置，没有轮询。应用图标为紫金 Lakers 24 像素画。

小米 SN 兼容在现有 ROOT 初始化中执行一次：读取 `ro.serialno`，回退读取
`ro.boot.serialno`，再同步到 `ro.ril.oem.psno` 与 `persist.custom.serialno`。主页直接
显示同步结果，也可手动刷新；没有后台定时检查，不改基带或 NV。

“LTPO 实际 TE 检测”将 `ltpo_test.sh` 封装为一次性 ROOT 终端，实时显示 15 秒采样，
25 秒超时后强制清理 `test_te`。它只在用户按下按钮时运行，不参与日常开机路径。

“综合功能诊断”分四层校验：

- system_server、SystemUI、设置、蓝牙、手机管家分别在真实进程入口加载后写入本次
  开机标记；system_server 在 `systemReady()` 尝试一次，并复用 12 秒 ROOT 启动任务
  补发一次，解决接收器尚未就绪造成的假 `WAIT`，不建立循环或常驻线程。
- ROOT 诊断检查 `f4_wake_bin`、FOD 内核桥、LTPO KO、SN 属性和必要 sysfs 节点。
- `liblhdcv5.so` 与 `liblhdcv5BT_enc.so` 分别执行真实 `dlopen`，不是只检查 APK 内文件；
  wrapper 另检查 `lhdcv5BT_get_handle`、`lhdcv5BT_init_encoder` 和
  `lhdcv5BT_encode` 三个关键编码符号。
- 传感器诊断确认一加移动、抬起和步数传感器在 `sensorservice` 中可见。依赖真实手势、
  指纹或耳机的最终行为仍需对应硬件触发回归。

一加 13、HyperOS `OS4.0.0.9.XPCCNXM`、Android 17/API 37 真机重启验证结果：上述全部
作用域、两份 LHDC so、wrapper 符号、ROOT 组件、内核模块、节点和传感器均显示
`[OK]`。设备当前页大小为 4096；核心 `liblhdcv5.so` 的 ELF LOAD 对齐为 4 KiB，适用于
当前内核，但若以后改用真正 16 KiB 页内核，必须换用 16 KiB 对齐的上游核心库。

Release 开启 R8 与资源收缩，同时保留全部模块类、Xposed 回调和 JNI 名称。v1.34
APK 为 5,918,498 字节，使用 APK Signature Scheme v2，`assembleRelease` 与
`lintVitalRelease` 均通过。

## 28. 澎湃震动增强完整合并

v1.35 将 `一加13澎湃震动增强-OS4模块和源码.zip` 的 `0.8.8-os4-test` 功能并入综合
模块，不再需要单独安装 `local.mio.oneplus13hyperhaptics`。运行时代码放在：

```text
app/src/main/java/local/mio/op13hyperosfix/haptics
```

原模块的完整源码快照放在：

```text
upstream/oneplus13_hyper_haptics_0.8.8
```

快照共 255 个文件，包含 Java、`rtp_root_helper.c`、构建文件、194 份 RTP、触感视频
音轨、RTP 分析/增益/重定向脚本和原始备份；不是只保留回编后的二进制。运行时 APK
包含 194 份 `.bin`，未压缩总计 27,933,732 字节，以及 597,920 字节的
`rtp_root_helper` 和 231,870 字节的 `haptic_video_192.ogg`。

震动链分为三层：

- `android/system_server`：放宽小米私有效果校验、扩展支持列表，将小米效果映射到
  一加效果；对需要原始波形的效果走 OS4 VINTF `IOplusVibrator` RTP 传输，镜像
  每效果强度并补齐进程内 `IVibratorExt` shim。
- `com.android.systemui`：补齐通知长按、清除通知和指纹成功/失败触感。指纹失败不再
  绑定固定的 R8 synthetic 数字后缀，而是按 FOD 包名、`MiuiGxzwAnimView`、
  `ExternalSyntheticLambda`、`run()` 和内部 `Vibrator.vibrate(24L)` 调用结构识别。
- `com.android.settings`：解析小米触感视频效果，并在页面退出时停止视频 RTP/音轨。
  v1.35 当时加入的 0.0 至 1.5 seekbar 补偿已在第 30 节由官方 OS4 双版本归一化逻辑
  取代。

这些目标类在当前 OS4 SystemUI/设置中保留语义类名，没有普通业务类混淆。兼容代码
避免依赖字段偏移和 synthetic 序号；各功能独立捕获失败，因此未来 OS4 某一页面改名
不会阻断 framework RTP 或其他作用域。作用域仍只有 `android`、
`com.android.systemui`、`com.android.settings`，没有新增常驻应用作用域。

功耗方面没有轮询、保活服务或空闲播放线程。短 RTP 直接在 system_server 内通过
VINTF 执行；`rtp_root_helper` 只在需要长效果 192 时按需启动，完成后退出。真机空闲
检查没有 helper 进程残留。

一加 13、HyperOS `OS4.0.0.9.XPCCNXM`、Android 17/API 37 重启实测：

- framework 输出 `System Framework effect bridge active`、`OS4 VINTF prebaked RTP
  transport active` 和 `OS4 in-process IVibratorExt shim created`。
- SystemUI 输出 `extended SystemUI haptics hook active`、`fingerprint auth hook active`；
  实际指纹成功命中效果 5、强度 2。
- 设置输出触感视频 parser 已激活；历史 0.0 至 1.5 seekbar Hook 不再用于当前版本。
- 实际播放 RTP 162，HAL 返回 callback ack，framework 收到完成回调并正常 stop；没有
  Hook 安装失败、异常或旧独立震动模块双重加载。
- 综合诊断中三作用域、194/194 RTP、helper、视频音轨、ROOT、FOD、LTPO、LHDC 和
  SN 全部为 `[OK]`。

最终 v1.35 Release 为 9,327,951 字节，只使用 APK Signature Scheme v2。Debug、
Release、`lintDebug` 和 `lintVitalRelease` 全部通过；最终 SHA-256 见第 14 节。

## 29. OS4 全局触感强度桥接

v1.37 修复 HyperOS 强度滑条只改变 framework 记录、但一加马达仍固定满幅的问题。
对照完整小米 OS4 ROM `E:\MIO\AAAOS4` 与 ColorOS 16 原生服务后确认：小米通过
`IVibratorExt.setAmplitudeExt(float, 0)` 向自家 HAL 写入归一化强度，而一加 HAL
不实现这份扩展；AOSP 的 `LOW/MEDIUM/HIGH` 只能改变统计中的 EffectStrength，不能
改变一加预制波形的最终驱动幅度。

ColorOS 的 qcom 线性马达官方链路会在每个预制效果前调用：

```text
setAmplitude(1 + rawStrength / 2400)
perform(waveformId, STRONG)
```

其中一加支持的预制效果原始强度为 `800..2400`。模块现在镜像小米的 USER、DIRECT、
每效果校准和 BCL 优先级，把归一化值换算为该范围，随后在
`VintfHalVibrator$DefaultHalVibrator.on(..., PrebakedSegment)` 的稳定传输边界执行
同样的 ColorOS 编码；旧版 `VibratorController$NativeWrapper` 路径保留等价兜底。

强度覆盖分三类，且共用 HyperOS 官方滑条：

- 小米私有短 RTP：继续通过 `IOplusVibrator` Binder gain 连续缩放。
- 普通预制效果（含 AOSP 0 至 12、输入法和一加原生回退）：使用上述 `800..2400`
  原始强度，随后固定以 `STRONG` 播放，避免 HAL 再次覆盖幅度。
- Step、Primitive、PWLE 与应用自定义波形：在 `VibrationScaler.getScaleFactor()`
  对 feedback usage 连续缩放；铃声、通知、闹钟和媒体用途保持原系统策略。

小米 OS4 的设置页把滑条保存为 `haptic_feedback_infinite_intensity`。当前 ROM 未声明
`sys.haptic.slide_version=2.0`，所以存储范围为 `0..1.5`；miui-services 在开机和设置
变化时除以 `1.5` 后调用 HAL。模块接收的正是 `0..1` 归一化值，因此不需要第二份
配置、ContentObserver、轮询线程或常驻服务，开机恢复与拖动滑条都会即时生效。

涉及文件：

```text
app/src/main/java/local/mio/op13hyperosfix/haptics/HookEntry.java
app/src/main/java/local/mio/op13hyperosfix/haptics/RtpPlayer.java
```

v1.37 Release 使用 Temurin 17、Gradle 9.3.1 和 Build Tools 36.0.0 完成 Java、R8、
资源优化、`assembleRelease` 与 `lintVitalRelease` 校验；APK 仅使用 v2 签名。

## 30. 小米官方滑条、场景强度与波形还原

v1.38 重新对照小米 OS4 设置、`miui-framework.jar`、`miui-services.jar`、小米
`vendor.xiaomi.hardware.vibratorfeature.service` 以及 ColorOS 16 一加振动服务，修正
v1.37 中仍不符合两边 HAL 语义的强度合成。

小米滑条有两套界面，但送进 HAL 的 USER 值相同：

- `sys.haptic.slide_version=2.0`：`MiuiHapticInfinitySeekBar` 直接把 `0..100` 保存成
  `0.0..1.0`。
- 旧版页面：设置保存 `0.0..1.5`，`miui-services` 调用 HAL 前除以 `1.5`。
- 因此两者最终都是 `0.0..1.0`。旧模块将新版结果再次乘 `1.5` 属于移植补偿，不是
  小米官方曲线，而且会压缩普通与强档的差距；v1.38 已移除该 Hook。

小米 HAL 的实际合成顺序为：

```text
DIRECT（合法时仅使用一次）
否则 USER × 当前效果校准值
最终限制到 0..1
BCL 生效且结果 > 0.4 时，再用 BCL 值替换
```

`miui.util.VibrateUtils` 对每个场景发送的校准值为弱 `0.375`、中 `0.625`、强
`1.0`。所以各处触感并不应该一样大：场景属性决定档位，RTP 文件或预制效果决定包络、
频率和时长。电源键息屏继续使用小米 `effect_209.bin`，兼容一加 0916T 马达的私有波形
继续直接 RTP。v1.38.2 取消了 `88/106/114/163/188/203/410` 的一加原生例外：复核资源
来源后发现其中只有部分编号真正等价，其余会被近似成普通点击，导致“趣味拟物”丢失
原有层次。现在所有已打包的小米私有效果都按各自资源播放，不再用编号相近的一加预制
效果替代。

一加 qcom 线性马达的有效预制强度为 `800..2400`，不能把小米零档翻译成真正的零输出。
新的换算为：

```text
raw = 800 + round(1600 × xiaomiFinalScale)
```

该范围也由 ColorOS 16 设置源码交叉确认：一加 13 声明
`oplus.software.vibrator_luxunvibrator`，`OplusTouchVibrationController` 将进度直接加
`800`，最大进度 `1600`，即原生触感滑条正是线性的 `800..2400`。因此弱、中、强仍
保持层次，而强档能够到达一加校准上限。短 RTP 使用等价的
`1/3 + 2/3 × scale` 增益曲线，并保留不同波形已有的安全峰值。实机日志证明四种输入法
都请求小米标准 `effect 7`。直接把小米 PCM16 波形以满幅 RTP 播放会令一加 13 马达破音，
因此 v1.38.3 不再打包 `effect1.bin`/`effect7.bin`，并把输入法 `effect 7` 映射到已实机
校准的一加原生 `effect 2`。AOSP 预制效果 `0..12` 其余编号也继续走一加原生路径，不再
被私有 RTP 资源覆盖。其他已调好的小米私有短 RTP、闭屏、指纹和通知长按仍使用各自
原有上限，不做全局加幅。零档保留马达最低触感；是否彻底关闭触感仍由系统“触感反馈”
开关控制。

ColorOS 对预制效果先调用 `setAmplitude(1 + raw / 2400)`，再以 `STRONG` 播放。
当前 qcom HAL 会先保存这个私有范围的强度，再按公开 AIDL 语义返回 unsupported；旧代码
把该返回值误判成失败，导致强度设置被放弃。v1.38 接受“值已写入、公开接口报不支持”
这一原生行为，并继续执行预制效果。v1.38.1 进一步直接发送同一基础 Binder 事务，不解析
这个预期的错误回复，避免 framework 为每一次触感打印 `Error setting amplitude`；真正的
Binder 传输失败仍会记录并回退。

Step、Primitive、PWLE 和普通振幅数组只针对 feedback usage 使用同一马达有效区间；
铃声、闹钟、媒体等用途不套用触感滑条。整个实现仍为事件驱动，没有轮询、常驻线程或
额外唤醒。v1.38.1 还关闭了 USER 每级变化、每次 RTP 播放、停止、callback 和完成通知
的高频成功日志，只保留初始化、首次映射和异常日志，避免拖动滑条或连续手势时产生
LSPosed 文件 I/O。

这里没有对 `/odm` 或 `/vendor` 做 bind mount。LSPosed 只在 `system_server` 的效果分流点
选择 APK 内的小米波形，随后通过系统现有振动 HAL 的 Binder IPC 发送 RTP；Binder 是
Android 进程间通信，不是文件挂载。主 RTP 事务失败时，haptic-A 备用路径也能读取同一份
有符号 PCM8 RTP 资源。两条路径均按请求触发，不增加轮询、保活服务或空闲线程。

## 31. 设置演示与笔记待办完成触感

OS4 设置的“趣味拟物”同时带有单马达和双马达 SRT。当前移植会错误选择双马达文件，
“笔记待办完成”因而请求标准 `effect 1`。模块只在
`com.android.settings.haptic.widget.HapticGridView.playExtPatternById(int)` 内把该请求
改为小米单马达 `effect 171`；不全局替换 `effect 1`，不会影响滑条、输入法或其他应用。

实际笔记应用不是这个调用点。继续对比 MIUI 14 与 OS4 的 `MIUINotes.apk` 后确认，旧版
明确区分两种语义：普通完成请求新协议 `effect 206`、旧协议回退 `167`；最后一项完成
请求新协议 `effect 205`、旧协议回退 `171`。OS4 把两条路径错误合并到了同一个
`i7.f.b()`，所以普通完成和全部完成都会请求 `206`。

两条路径仍有稳定且不依赖混淆类名的区别：普通完成在主线程直接调用；全部完成由应用
把执行线程明确命名为 `todoAniWorker` 后调用。2026-08-21 的一加 13 实机日志在同一时刻
记录了 `TodoAnimHelper doAllFinishAnim`、`caller=todoAniWorker` 和 `rtp:206`，证实这正是
OS4 的回归点。v1.42.0 在推荐作用域中新增 `com.miui.notes`，只 Hook 未混淆的系统 API
`miui.util.HapticFeedbackUtil.performExtHapticFeedback(int)`：仅当线程名为
`todoAniWorker` 且参数为 `206` 时改为 `205`。主线程的普通完成、设置演示、其他应用和
其他 effect 均不受影响。

移植包原有的 `effect_206.bin` 含两段约 10 ms 的小米专用载波，中间为约 79 ms 数字静音。
小米马达的机械余振会把这段反馈融合，一加 13 马达制动更快，静音区会被感知成两次独立
震动。v1.38.3 保持 `206` 私有 ID、原载波、相位和峰值，只删除中间静音并在接近零点处
衔接，得到 539 字节、约 22.46 ms 的连续专用反馈；没有映射到一加工厂效果，也没有发起
第二次振动。可复现生成脚本为：

```text
tools/build_notes_todo_rtp.ps1
```

最终 `effect_206.bin` SHA-256：

```text
B2A704445E418D7269995B693C3F395F59BF36C03473F649E4DCBFB52A451B38
```

全部完成最终播放 APK 内已有的 `effect_205.bin`；它与已实测正确的长反馈
`effect_171.bin` 相同，SHA-256 为：

```text
884ACDC922E8F3C54455C17227B73425CC77F55D64CF5E8B578C50BAFD6BF098
```

v1.42.0 使用 Temurin 17、Gradle 9.3.1、Build Tools 36.0.0 完成
`compileReleaseJavaWithJavac assembleRelease`。实现仍完全由触发事件驱动，没有轮询、
常驻播放线程或空闲 CPU 开销。

## 32. Android 17 支付宝 IFAA 指纹支付

OS3 已修复的 `IFAAService.apk` 与原版对比表明，一加 13 指纹支付需要同时满足三项：

- IFAA 型号返回 `ONEPLUS-R23821`，支持类型返回 `17`。
- 已录入 ID 直接来自 `FingerprintManager.getEnrolledFingerprints()`。
- TEE 命令转发到
  `vendor.oplus.hardware.biometrics.fingerprintpay.IFingerprintPay/default`，接口描述符为
  `vendor.oplus.hardware.biometrics.fingerprintpay.IFingerprintPay`，命令事务为 `6`。

OS4 的 IFAA APK 已重写并混淆，不能继续依赖 OS3 的方法名。当前 Hook 在
`org.ifaa.aidl.manager` 进程按稳定的 Binder 事务号工作：事务 `1` 返回支持类型，事务 `3`
返回设备型号，事务 `4` 转发 TEE 命令，事务 `9` 返回已录入指纹 ID。HAL 连接使用
`ServiceManager.getService()`，只在命令到达且服务暂时缺失时进行最多 6 次、间隔 10 ms 的
短重试；连接成功后缓存 Binder，死亡时失效，不存在后台轮询。

Android 17 还新增了实际阻断链路的两层权限：

1. `FingerprintService.canUseFingerprint(...)` 会校验 `USE_FINGERPRINT`、`USE_BIOMETRIC`
   和 AppOps。模块在 `system_server` 内只对调用包严格等于
   `org.ifaa.aidl.manager`、且 Binder 调用 UID 确实拥有该包时返回允许；其他应用和其他
   生物识别调用保持原系统逻辑。
2. IFAA 当前运行在版本化域（实机为 `platform_app_36`），但一加 HAL 只允许
   `hal_fingerprintpay_client`。ROOT 启动逻辑从 `ps -AZ` 动态读取 IFAA 实际域，校验域名
   只能是 `platform_app[_数字]` 或 `system_app[_数字]`，再通过 KernelSU 的
   `ksud sepolicy patch`（Magisk 使用 `magiskpolicy --live`）加入
   `hal_fingerprintpay_client` 属性。不能把规则写死为 `platform_app_36`，否则 IFAA 的
   targetSdk 或 SELinux 版本化域在 OTA 后改变就会再次失效。

SELinux live patch 在重启后会消失，因此模块通过既有的 system_server 开机广播和打开
应用时的 ROOT 初始化重新应用；它不是 `service.d`，没有常驻 shell、睡眠进程或定时检查。
LSPosed/Vector 作用域必须包含 `system`（即 system_server）和
`org.ifaa.aidl.manager`。前者提供受限的指纹权限放行及开机 ROOT 引导，后者完成 IFAA
返回值和 TEE 命令桥接。

ColorOS 16 的 `OplusExSystemService` 也通过同一个 `IFingerprintPay` HAL 转发支付宝命令，
因此该路径与一加底层一致；但本机 ColorOS 配置没有一加 13 的支付宝人脸型号条目，OS4
小米设备特征又声明 `ifaa_2dfa_support=0`。当前版本只声称并修复已实测成功的指纹支付，
没有伪造人脸支持。

实机在一加 13、HyperOS 4 Android 17 DSU 上验证支付宝指纹恢复。对应成品为
`OP13HyperOSFix-v1.39.1-ifaa.apk`，`versionCode 44`，SHA-256：

```text
FBAA19F37097526C0B0275CC427945F0B11C701352353662140C5C0233509B0C
```

构建使用 Temurin 17、Gradle 9.3.1 和 Build Tools 36.0.0；`lintRelease` 无 Error，APK
通过 v2 签名校验。IFAA 修复全部按 Binder 请求或开机事件触发，空闲时没有额外 CPU 与
唤醒开销。

## 33. DownloadProvider 开机防杀与 MTP 稳定性

HyperOS 4 的 `com.android.providers.downloads` 运行在 `android.process.media`。实机
`DownloadProvider.apk` 中，`BootHelper` 会在开机后的云控时间窗口内启动
`DMS_BootEvent-KillThread`，每 60 秒检查一次活跃状态；判定为空闲时会调用
`XCrashlytics.killSelf()` 杀死承载下载与媒体相关服务的进程，可能连带造成 USB/MTP
连接异常。

模块完整接入 `DownloadProvider Boot Guard v1.1` 的分层保护：

- `CloudConfigPreference.getBootKillTimeout()` 固定返回 `-1`，setter 和仅匹配
  `boot_kill_timeout` 的 `SharedPreferences` 兜底也只写入 `-1`。
- `BootHelper.inKillPeriod()` 返回 `false`，`checkProcessKill()` 不再进入自杀路径。
- `XCrashlytics.killSelf()`、`Process.killProcess/sendSignal` 以及
  `System/Runtime.exit` 仅在 `DMS_BootEvent` 线程或已确认的 `BootHelper` 调用链中拦截。
  普通崩溃处理、外部杀进程和其他 DownloadProvider 云控字段保持原逻辑。

推荐作用域新增 `com.android.providers.downloads`。所有 Hook 都在目标方法调用时触发，
没有服务、轮询、定时唤醒、ROOT 命令或日志读取；`BootHelper` 自带的 60 秒线程会因超时
固定为负数而不创建，因此相比原系统还少一个开机短时线程。原版完整源码保存在
`upstream/downloadprovider_bootguard_v1.1`，便于 OTA 后核对类名和签名。

## 34. MiRemix 来电随音震动波形修复

一加 13 实机来电日志表明，`MiRemix.ogg` 不是普通的预制 effect，而是由
`Ringtone` 启动音频后通过 `ExternalVibration` 播放随音触觉通道，振动用途为
`USAGE_RINGTONE`（framework usage `33`）。因此全局 RTP 均衡表、输入法强度桥和普通
`PrebakedSegment` 映射都不能安全地只修这一首铃声。

v1.42.0 曾在 `system_server` 中把该铃声的
`ExternalVibrationScale.scaleFactor` 从 `1.0` 改为 `0.76`。LSPosed 日志和
`dumpsys vibrator_manager` 都证明 Hook 确实命中，但实机破音没有改善；铃声循环重新启动
外部振动时还可能离开原来的 2 秒识别窗口。因此问题不是普通强度档位，而是 OS4 文件内
嵌触觉波形与一加 13 马达不匹配。

`ffprobe` 和逐声道频谱分析确认：OS4 文件是 48 kHz、三声道 Vorbis，带
`ANDROID_HAPTIC=1`。左右两路为可听铃声，额外一路为同步触觉数据。OS4 触觉声道几乎
持续铺满高频，解码后的过零率为 `0.200650`；MIUI 14 三声道 Opus 版本是稀疏节奏脉冲，
过零率为 `0.009424`。后者通过临时 bind 挂载在一加 13 上完成来电实测，节奏正常且不再
破音。该次测试持续约 32.6 秒并跨过一次铃声循环：第一段 19.7 秒仍带旧版 `0.76`
倍率，第二段 12.9 秒为 `1.00` 原始倍率，两段均正常，因此最终版不再需要倍率 Hook。

v1.43.0 删除全部 MiRemix URI 解析、播放时间窗口和
`ExternalVibrationScale` Hook，改为冷挂载验证通过的文件：

```text
system/product/media/audio/ringtones/MiRemix.ogg
SHA-256: 2358D0A86134B69E13CCE3F5E5F45CBAF53BB5690138D540295117EA017DBC69
```

该方案不改变其他铃声、输入法、系统触感、指纹或笔记震动，也没有日志监听、轮询、常驻
线程或额外唤醒；运行时 CPU 开销为零。

## 35. 系统应用与 native 库固化

只把 APK 放进 `/product/app/OnePlus13HyperOSFix` 会令
`ApplicationInfo.nativeLibraryDir` 指向该目录下的 `lib`，但系统扫描预装应用时不会替
模块自动从 APK 解压 native 库。结果是 LHDC 诊断中的核心库、wrapper、注入桥和
DexKit OTA 兼容库均会 `dlopen` 失败。v1.44.0 固化模块使用 Mountify 兼容层级同时提供：

```text
system/product/app/OnePlus13HyperOSFix/OnePlus13HyperOSFix.apk
system/product/app/OnePlus13HyperOSFix/lib/arm64/libandroidx.graphics.path.so
system/product/app/OnePlus13HyperOSFix/lib/arm64/libdexkit.so
system/product/app/OnePlus13HyperOSFix/lib/arm64/liblhdcv5.so
system/product/app/OnePlus13HyperOSFix/lib/arm64/liblhdcv5BT_enc.so
system/product/app/OnePlus13HyperOSFix/lib/arm64/libop13_lhdc_patch.so
system/product/media/audio/ringtones/MiRemix.ogg
```

安装脚本校验 APK、五个 so 和 MiRemix 文件的 SHA-256，并设置目录 `0755`、文件
`0644`。模块不包含
`service.sh`、`post-fs-data.sh`、热替换或常驻进程；KernelSU 用户需要安装并启用
Mountify 元模块，重启后完成冷挂载。新版 APK 与当前系统应用使用同一证书，证书
SHA-256 为：

```text
47F1BF86449C4D3021DAD441982204C0F8531FDC388E1251ECB31FB9D04A3A4C
```

固化模块成品：

```text
out/OP13HyperOSFix-SystemApp-v1.44.0.zip
SHA-256: C43D37686B5E228C353BEE412D8C7B5B05B689A6B8AEC31D3F8C143A433D64E3
```

## 36. “我的设备”参数与页面美化

v1.44.0 将 `COS_OS4_Device_Params_LSPosed_v2.5` 的运行逻辑合入综合修复，但没有
沿用其 Java 配置界面。配置入口使用现有 Compose/Material 3 组件重写，位于主页面
“交互体验”区域，保留五个开关和七项可编辑设备参数：

- 美化总开关：默认开启。
- 强制定制深色：默认开启；与通用定制双色互斥。
- 通用定制双色：默认关闭。
- 页面音乐：默认开启，只在“我的设备”页面前台期间播放，退出时淡出并恢复媒体音量。
- 设备参数注入：默认关闭；机型、处理器、电池、分辨率、屏幕尺寸和前后摄像头仍可编辑。

Hook 作用域只使用项目已有的 `com.android.settings`。加载时优先匹配 OS4 的稳定类名：

```text
com.android.settings.device.DeviceParamsManager
com.android.settings.device.MiuiMyDeviceSettings
com.android.settings.device.controller.BgEffectDataManager
com.android.settings.device.MiuiAboutPhoneUtils
com.android.settings.device.DeviceServiceCache
```

某个类或方法被 OTA 改名时，才使用稳定字符串指纹短暂打开 DexKit 搜索 Settings APK，
搜索结束立即关闭 bridge；精确类名仍存在时不会启动 DexKit。配置通过综合修复应用的
`ConfigProvider` 提供给 Settings，Provider 仅允许自身 UID 和
`com.android.settings` 的 UID 查询，其他调用者直接拒绝。读取失败时所有 Hook 均
fail-open，设备参数和页面接管都不生效，不会破坏设置应用原行为。

该功能没有日志读取、定时器、后台服务或持续扫描。页面创建时只有两次短延迟布局刷新；
页面退出后音乐释放，空闲时没有新增 CPU 占用。Release APK 为
`versionCode 55 / 1.44.0-device-page`，APK SHA-256：

```text
84C42D80F58587124C0E57862154C2D40824D6D27A9BADFE48BB5D97D953173A
```

## 37. 小米换机与云备份 WLAN 兼容

v1.45.0 合入 `XiaomiWlanCompatLSP`，运行逻辑迁入
`local.mio.op13hyperosfix.wlancompat`，配置页使用综合修复现有的
Compose/Material 3 组件重写。新增推荐作用域：

```text
com.miui.huanji
com.miui.cloudbackup
```

AAAOS4 中 `MIUIHuanji.apk` 的 application 同时声明
`defaultToDeviceProtectedStorage=true` 和 `directBootAware=true`。模块在 zygote 中只安装
一次 `ActivityThread.handleBindApplication` 回调，且仅在绑定包名为
`com.miui.huanji` 时清除对应 `ApplicationInfo.privateFlags`，再把 `dataDir` 指向
`credentialProtectedDataDir`。这保证 Context 创建前已经切回 CE 用户目录，不依赖换机
应用内部混淆类名；其他应用只经过一次包名比较。

AAAOS4 与 OS3 的 `MIUICloudBackup.apk` 都使用
`com.miui.cloudbackup.infos.DeviceInfo.b()/c()` 获取并缓存 `wlan0` MAC。模块先安装这两个
精确 Hook；若 OTA 改变类名或方法名，则使用 `get wlan0 interface info failed` 和
`02:00:00:00:00:00` 两个稳定字符串、零参数静态 `String` 返回类型进行 DexKit 兜底。
只有精确 Hook 全部失效时才打开 DexKit，使用两个线程完成一次搜索后立即关闭。

真实地址读取顺序为 sysfs 直读、按需 `su` 读取、已有真实缓存，全部失败时生成与
Android ID 绑定的稳定本地管理地址。接口名限制为固定白名单，不接受 shell 注入；
Provider 优先校验 Binder 调用包名，只允许模块自身、小米换机和云备份，系统无法提供
调用包时才退回 UID 包列表。UI 不显示具体 MAC，只显示缓存来源。

该功能没有日志读取、常驻进程、后台服务、定时器或持续轮询。ROOT 最长 2.5 秒等待只在
用户点击“读取并缓存真实地址”或云备份首次缺少真实缓存时发生；成功后进程内和
SharedPreferences 双层缓存。Release APK 为 `versionCode 56 / 1.45.0-wlan-compat`，
APK SHA-256：

```text
B26E5A2028A60E5024F61B8F796A59521C6A6F49CEC97762EF168CBA481AB354
```

系统应用固化模块：

```text
out/OP13HyperOSFix-SystemApp-v1.45.0.zip
SHA-256: F996E88A354ECF32545A929EBDAD7CE1B65D575CD31C44681F5DA7F37F42C670
```

## 38. 触感上限、返回手势与日常触控采样率

v1.51 保留小米官方触感滑条的数据源和各场景原有强弱标准，不另建第二个触感开关。
一加原生预制效果仍限制在 ColorOS 标定的 `800..2400`，避免超范围驱动导致破音；小米
私有 RTP、Step、Primitive 和 PWLE 的高档上限扩展到 130%，低档起点保持不变。
返回手势使用的 `effect 162/163` 单独提高约 60%，通过 Binder 增益和有符号 PCM 饱和
缩放共同完成，不重复播放、不延长波形，也不影响输入法、通知、来电或 MiRemix。

“日常触控采样率”仅采用 Sense 的一加触控固件命令：

```text
/odm/bin/touchHidlTest -c wo 0 182 <70|120|180|240|360>
```

UI 使用离散 Material 3 滑条，默认 120 Hz。选择 180 Hz 以上时，亮屏使用所选档位，
息屏写入 70 Hz；70/120 Hz 则固定使用所选档位。开机初始化复用原有 ROOT bootstrap，
亮屏和息屏切换复用 `PhoneWindowManager` 已有生命周期 Hook，通过显式广播让应用进程
执行一次 `su` 命令。没有常驻服务、getevent、日志读取、定时器或轮询，屏幕状态稳定
期间没有新增 CPU 开销。工具不存在或 ROOT 命令失败时只跳过本次写入，不阻断其他修复。

## 39. 按应用游戏触控与旁路供电

v1.52 增加独立的“游戏触控与旁路供电”配置页。游戏由用户从可启动应用列表中选择，
配置只保存包名，因此游戏升级不会导致匹配失效。游戏触控可选
`70/120/180/240/360 Hz`；所选游戏可见时写入游戏档位，离开后恢复日常档位，息屏仍按
第 38 节规则回到低档。

前台识别运行在 `system_server`，优先 Hook AOSP 稳定入口
`ActivityRecord.onWindowsVisible/onWindowsGone`，以可见 Activity 集合兼容分屏和游戏内
Activity 切换；OTA 缺少任一入口时退回
`ActivityTaskManagerService.setLastResumedActivityUncheckLocked`。退出采用一次 800 ms
延迟以跨过同一应用 Activity 切换，新 Activity 出现会立即取消延迟。没有无障碍服务、
UsageStats、进程扫描或周期轮询。

旁路沿用 ColorOS 充电 HAL 的节点语义：

```text
/sys/class/oplus_chg/battery/mmi_charging_enable
fallback: /sys/class/power_supply/battery/mmi_charging_enable
0 = 旁路/停止向电池充电
1 = 正常充电
```

有线状态在 ROOT 应用进程中再次校验，ColorOS 16.10 优先读取
`/sys/class/power_supply/usb/online`，旧底层再回退到
`/sys/class/oplus_chg/battery/wired_online` 和 `/sys/class/power_supply/ac/online`。
这项二次校验只在状态切换命令执行时读取一次，不会持续访问 sysfs。

旁路只在所选游戏可见、有线充电、电量不低于 15% 且达到进入温度时开启。进入温度可在
30–49°C 设置，退出温度固定为进入温度减 4°C，防止临界温度附近反复切换。退出游戏、
息屏、拔线、电量过低或温度降到退出阈值时恢复正常充电。模块仅在自己把节点从 1 写到
0 后创建 `/data/adb/op13_hyperos_fix/game_bypass_owned`；恢复时也只处理带该所有权标记的
状态，不覆盖其他旁路工具。关闭功能后再次打开应用时还会清理遗留标记并恢复节点。

可选调试 Toast 只在“非游戏状态进入所选游戏”的边沿触发，不在游戏运行期间刷新。
Toast 会显示触控命令是否成功、写入档位、旁路节点实际回读值，以及当前温度与进入/退出
阈值的判定结果。稳定运行时只保留 Activity 可见性回调和系统电池广播，状态未变化不会
启动应用进程或执行 `su`，因此没有常驻线程和额外轮询开销。

ColorOS 16.10 节点兼容和两行调试 Toast 的最终测试版本为
`versionCode 65 / 1.52.2-game-bypass-final`。实机验证退出游戏后
`mmi_charging_enable=1`，进入所选游戏后为 `0`；Release APK SHA-256：

```text
C50CF541FA1B4CEADEB31EA2FD232F0D162ED922382DC60826E1FDDCFD9ADF74
```

## 40. 内置调度、线程放置与 FAS

v1.56 把 Scene 的离线调度边界重做成模块自己的实现，不依赖安装 Scene，也不读取云端
配置。ROOT 文件统一放在：

```text
/data/adb/op13_hyperos_fix/sched
games.conf       本地游戏名单和档位
normal.conf      普通应用覆盖档位
game_fas.conf    每个游戏最终生效的 FAS 开关
scheduler.conf   调度开关
thread.json      内置线程规则
fas.state        FAS 当前状态
visible_apps.state
```

`SceneSchedulerBridge` 通过 HyperOS `ProcessManager` 主前台/焦点回调、自由窗口回调和系统
电源事件获得状态。普通前台识别没有进程扫描、UsageStats 或周期轮询。相邻 Binder 事件只
做一次 80 ms 合并；游戏进入后再做一次 500 ms 侧边栏复核，避免 Game Turbo 设置提交比
前台回调稍晚。

内核模块 `op13_scene_sched.ko 0.6.0-fas` 使用 `freq_qos` 保存 CPU0 和 CPU6 两个物理
cpufreq policy 的最低/最高票，通过 `/proc/op13_scene_sched` 一次提交完整的模式、配置族、
省电、温控、亮屏和所有者状态。温控与省电始终是硬上限，游戏和 FAS 都不能将其抬高。
配置锁只在亮屏时每 10 秒校验容易被厂商服务覆盖的 WALT、cpuset、core_ctl、GPU
`mod_percent` 等辅助节点；CPU QoS 由内核自身保持。

线程放置使用 `thread.json` 中的主线程、RenderThread 和重负载线程规则。用户态只在
前台边沿提交进程和已存在的 TID；KO 只在确实管理游戏进程时注册 task rename/newtask
通知，退游戏立即释放亲和性并注销通知，所以桌面没有线程创建热路径开销。

FAS 使用一加/高通显示驱动提供的硬件帧计数：

```text
/sys/class/drm/card0/card0-sde-crtc-0/measured_fps
格式示例：fps: 117.2 duration:500000 frame_count:59
```

只有 `games.conf` 中的应用可见、该应用的 FAS 策略允许且屏幕点亮时才读取该 sysfs。
活跃画面每 250 ms 采样，低方差稳定画面每 500 ms，静止画面降到 1000 ms；退出游戏后
不再调度采样。前六个活跃样本学习 30/45/60/90/120/144/165 FPS 档位，并使用指数平均
与帧方差建立动态余量。明显掉帧立即恢复模式上限，连续低于方差余量时逐级升档；只有
低方差稳定满 6 秒才每次降低一个集群档位。温控进入 warm/hot 后同步下调有效目标帧率，
但最终仍受内核温控硬上限约束。更高帧档需要连续四个样本确认，避免通知、悬浮窗或加载
动画的一次峰值错误提升目标。

各模式 FAS 初始上限沿用本机频率表和 Scene 离线预设：

```text
省电：CPU0 1785600  CPU6 2438400
均衡：CPU0 1996800  CPU6 2649600
性能：CPU0 2400000  CPU6 3072000
极速：CPU0 2400000  CPU6 3283200
```

FAS 只提交更低的最高频票，不改变最低频率。KO 最终执行
`min(模式上限, 温控上限, FAS上限)`；非游戏、灭屏或桥退出时普通策略提交还会再次清除
FAS 票，避免异常退出留下锁频。UI 的全局 FAS 是默认值，每个游戏还可单独选择继承、
开启或关闭；显式开启可覆盖全局关闭。状态可从 `fas.state` 和
`/proc/op13_scene_sched` 的 `fas` 行检查。

一加 GameOpt 的 `/proc/game_opt/cpu_max_freq` 与 `cpu_min_freq` 可通过 UI 开关隔离。
隔离使用独立 bind mount 所有权记录，只卸载自己创建的挂载。`scheduler.conf` 默认内容：

```text
LOGGING=off
THREAD_PLACEMENT=on
CONFIG_LOCK=on
FAS=on
DAILY_MODE=balance
```

日志关闭时是直接返回的快路径，不做时间戳、文件 stat 或写盘。桌面仍只有配置锁的低频
校验；FAS 的 4/2/1 Hz 动态采样只存在于允许 FAS 的可见游戏。退回桌面后回调会移除，
`fas updates` 不再增加。

## 41. Metric 式悬浮监控与双电芯日常统计

性能监控由用户主动打开，ROOT 采样器只在前台服务存活期间运行。通知入口弹出四种窗口：

```text
细条：CPU/GPU 占用、频率和整机功耗
总览：三个 Metric 式圆环显示 CPU、GPU、双电芯功耗与温度
进程：按 CPU 排序的进程列表和 RAM
线程：当前前台应用 TID、线程名和允许 CPU
```

窗口打开和收起使用 120–180 ms 的一次性淡入/缩放动画，数值每秒直接重绘，不使用常驻
动画。拖动限制在屏幕内；关闭窗口会停止服务、Root 子进程和实时采样。四种模式切换复用
同一个采样器，不会累积进程。

日常耗电统计是单独的 10 秒低频采样器，按双电芯包电压计算功率。USB 在线、无线供电、
系统电池状态不是 Discharging 或旁路供电时，重置积分基线并显示“外接供电/旁路中”，
不把充电功率计入耗电。Android 17 不允许普通后台 Receiver 直接启动 FGS，因此开机
ROOT bootstrap 在已获得 ROOT 后用系统身份执行一次 `am start-foreground-service`；
未开启日常统计时不执行。

最终 APK：`versionCode 73 / 1.56.3-pickup-setting`

```text
app/build/outputs/apk/release/app-release.apk
SHA-256: 547707525674B4D4E7FECE464C61FCD8BCC893E775C13D07BFBB6C1F66AA1FEB

kernel-src/op13_scene_sched.ko
SHA-256: 3211B8C9379463CF59655BF0E359FFA735E278B12A1F521B1C72EFA9E24DF4E2
```

## 42. 通知悬浮选择器与游戏名单快速编辑

日常耗电和实时监控通知不再使用 `PendingIntent.getActivity()`。点击通知会把显式动作发送给已经运行的前台服务，由服务直接创建 `TYPE_APPLICATION_OVERLAY` 模式选择器，因此不会拉起应用任务，也不会改变当前前台应用。选择细条、总览、进程或线程后才启动实时 Root 采样；关闭选择器不会启动采样。

选择器使用固定尺寸、8 dp 圆角和 160 ms 一次性淡入缩放动画。模式图标改为轻量单色符号，不使用 Android 早期内置位图图标。窗口监听外部触摸和关闭按钮，服务销毁时也会立即移除窗口，避免残留遮挡。

游戏应用名单不再在每次点击时锁住整个页面。点击先更新 Compose 本地状态，连续选择使用 250 ms 防抖合并，最后一次结果才进入单线程 Root 写入队列；调度服务同步同样只在合并写入后执行。Activity 销毁前会把仍在等待的最后一次结果提交到队列，再使用 `shutdown()` 等待已提交任务完成，避免快速返回时丢失选择。

真机连续三次勾选耗时 172 ms，连续三次取消耗时 188 ms，全部点击均被识别；最终 `op13_fix_game_packages` 精确恢复测试前内容。

## 43. 抬起亮屏严格服从系统开关

旧实现为了保证首次适配成功，在 `KeyguardSensorInjector` 构造完成后直接写入：

```text
Settings.System pick_up_gesture_wakeup_mode = 1
```

同时，替换后的 `updatePickupSensorRegistration()` 只判断屏幕交互和锁屏状态，没有读取这个设置。因此用户关闭开关后传感器仍然有效，SystemUI 在重启或开机重建时还会把设置重新写回开启。

v1.56.3 删除所有对该设置的写入。模块仍然只强制声明 `support_gesture_wakeup` 和 `isSupportPickupWakeup=true`，用于显示系统原有开关；是否注册一加 type 22 `pick_up_motion Wakeup` 则每次读取当前用户的 `pick_up_gesture_wakeup_mode`。平台当前用户读取 API 在 Android 17 SDK 中属于隐藏成员，因此运行时反射调用，失败时回退到进程用户的普通 `Settings.System.getInt()`。

最终注册条件为：

```text
系统抬起亮屏开关已开启
并且
屏幕非交互 或 锁屏显示且本次由抬起唤醒
```

真机验证中，开关为 1 且完全息屏时 SystemUI 注册 type 22；息屏期间改为 0 后约 2 秒内注销。SystemUI 进程重建后设置仍保持 0，不再由模块强制打开。

## 44. CNE 数据链防崩与逐应用调度策略

高通 CNE 修复作用域仅增加 `com.qualcomm.qti.cne`。Hook 位于
`ConnectivityManager.unregisterNetworkCallback(NetworkCallback)` 的返回点，只有同时满足
以下条件才清除异常：

```text
异常类型是 IllegalArgumentException
异常消息严格等于 NetworkCallback was not registered
堆栈包含 DataCallAgent$Tracker.destroyNetworkRequest
```

原方法已经执行完成，守卫只把这一次已核验的重复注销视为清理成功。其他调用方、其他异常
消息和其他堆栈全部保持原行为。该 Hook 没有服务、线程、轮询、文件或 ROOT 操作。

游戏页面的每个可启动应用现在包含三项策略：游戏身份、应用档位和 FAS。游戏身份决定使用
KO 的 game 或 app profile；档位选择“跟随”时，游戏继承全局游戏档，普通应用继承全局
日常档。普通应用的显式档位写入 `normal.conf`，游戏写入 `games.conf`。FAS 仅在游戏身份
下生效，可选择跟随全局、强制开启或强制关闭。

应用侧把显式覆盖紧凑存放在 `op13_fix_game_app_policies`，与游戏名单在同一个 ROOT shell
事务中提交。连续点击仍使用 250 ms 防抖，最后一次状态生成三份配置文件；调度桥只在文件
变更或前台可见性事件发生时重新决策，不增加按应用轮询。KO 不解析包名，只接收用户态已
仲裁好的档位与 app/game profile 原子票，这与旧版 `apps.conf` 后来收敛为 `normal.conf`
的架构一致。

本版：`versionCode 74 / 1.57.0-app-policy-cne`
