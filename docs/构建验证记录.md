# 构建验证记录

验证日期：2026年9月20日。

## 验证对象

- 版本：`1.58.0-coloros-wallet`
- versionCode：75
- 源码提交：`990a68e18abfc016cba00e311323601197a03b8f`
- 项目：`OP13HyperOSFix`

该提交之后补充的是文档，没有改动应用源码和构建配置。

## 检查结果

1. 发布副本的 `app/src`、`app/libs` 和 `native-src` 共 304 个文件，与原工作工程按 SHA-256 比较，未发现缺失或内容差异。
2. 钱包核心 `HookEntry.kt` 哈希仍为 `E4B181CCEAEDDC5CCB0D9C29AB0BCD55B9B0985F0F6664894CE6002BB66AF3BA`，与原合并审计一致。
3. Git 保存了构建需要的本地 JAR、AAR、SO、KO、RTP 和其他预编译辅助程序。
4. 发布副本执行 Release 构建成功。APK 内全部 205 个 assets 文件存在，LHDC 原生补丁及两项随包库存在。
5. 仅从 Git 提交重新克隆到全新目录，没有复制 `local.properties` 或原构建目录。
6. 新目录执行以下命令，退出码为 0，结果为 `BUILD SUCCESSFUL`，54 个任务全部执行，总用时 2 分 29 秒。

```powershell
.\gradlew.bat :app:assembleRelease --no-daemon --no-build-cache
```

产物版本检查为 `1.58.0-coloros-wallet`，versionCode 75。

## 环境与边界

实际验证使用 Windows、Temurin 17、现有 Android SDK 和 NDK 28.2.13676358，通过 `ANDROID_HOME` 定位 SDK。Maven 依赖下载缓存可以复用，但没有使用项目构建输出或 Gradle 构建缓存。

这说明仓库文件可以独立构建 APK，不代表无需安装工具链，也不代表预编译 KO 和厂商库都能从零重建。本次没有刷入或重启手机，没有重新验证全部系统功能。

不同目录、签名和构建元数据可能使 APK 哈希不同，本次没有声称构建结果逐字节可复现。
